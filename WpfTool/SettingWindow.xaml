<Window x:Class="WpfTool.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTool"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="SettingWindow" Height="450" Width="800">
    <Grid>
        <materialDesign:Card>
            <TabControl Width="Auto" materialDesign:ColorZoneAssist.Mode="PrimaryMid" Style="{StaticResource MaterialDesignFilledTabControl}">
                <TabItem Header="常规">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <CheckBox Content="开机启动" Style="{StaticResource MaterialDesignCheckBox}" Grid.Row="0" Grid.Column="0" />
                        <Label Grid.Row="3" Grid.Column="0" Content="取词间隔" ToolTip="划词翻译实现原理是：用户用鼠标划词后模拟Ctrl+C，间隔一定时间后，程序再从剪切板取词，如果电脑反应慢，可能会出现取不到词的问题，将间隔调大可以适配一些老旧的机器" />
                        <Slider x:Name="WordSelectionIntervalSlider" Value="200" Minimum="200" Maximum="1000"
                                Grid.Row="3" Grid.Column="1"
                                Style="{StaticResource MaterialDesignDiscreteSlider}"
                                TickFrequency="100"
                                TickPlacement="BottomRight" />
                        <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding ElementName=WordSelectionIntervalSlider, Path=Value, StringFormat={}{0}ms}" VerticalAlignment="Center" Margin="10,0,0,0" />
                    </Grid>
                </TabItem>
                <TabItem Header="文字识别">
                    <GroupBox Grid.Row="0" Grid.Column="0" Header="默认文本识别" Margin="16" />
                </TabItem>
                <TabItem Header="机器翻译">
                    <GroupBox Grid.Row="0" Grid.Column="0" Header="默认机器翻译" Margin="16" />
                </TabItem>
                <TabItem Header="全局热键">
                    <TextBlock Margin="8" Text="全局热键" />
                </TabItem>
                <TabItem Header="关于">
                    <TextBlock Margin="8" Text="关于" />
                </TabItem>
            </TabControl>
        </materialDesign:Card>
    </Grid>
</Window>
